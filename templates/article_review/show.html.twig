{% extends 'base.html.twig' %}

{% block title %}ArticleReview{% endblock %}

{% block body %}
    <h1>ArticleReview</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ article_review.id }}</td>
            </tr>
            <tr>
                <th>Review</th>
                <td>{{ article_review.review }}</td>
            </tr>
            <tr>
                <th>Author</th>
                <td>{{ article_review.author }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('article_review_index') }}">back to list</a>

    <a href="{{ path('article_review_edit', {'id': article_review.id}) }}">edit</a>

    {{ include('article_review/_delete_form.html.twig') }}
{% endblock %}
